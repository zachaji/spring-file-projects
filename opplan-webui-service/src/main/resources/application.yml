server:
  port: 8080

spring:
  application:
    name: opplan-webui-service

opplan:
  service:
    url: http://localhost:8088
    download-endpoint: /api/files/download

logging:
  level:
    com.opplan.webui: INFO
    reactor.netty: WARN
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"
